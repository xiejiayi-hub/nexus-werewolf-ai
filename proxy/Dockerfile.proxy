# 基础镜像
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 先复制依赖文件
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制当前目录的所有文件（因为Dockerfile就在proxy/里）
COPY . .

# 暴露代理服务的端口
EXPOSE 8001

# 启动命令（端口改回8001）
CMD ["uvicorn", "api_proxy:app", "--host", "0.0.0.0", "--port", "8001"]